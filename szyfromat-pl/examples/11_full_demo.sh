#!/bin/bash
# ═══════════════════════════════════════════════════════════════
# 11_full_demo.sh - Pełna demonstracja systemu e-Doręczeń
# ═══════════════════════════════════════════════════════════════

API_URL="${API_URL:-http://localhost:8500}"

echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║     e-Doręczenia SaaS - Pełna demonstracja                   ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
echo ""

# Funkcja do pauzy
pause() {
    echo ""
    read -p "Naciśnij ENTER aby kontynuować..." dummy
    echo ""
}

# ─────────────────────────────────────────────────────────────
# 1. LOGOWANIE
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 1: Logowanie do systemu                               │"
echo "└─────────────────────────────────────────────────────────────┘"

RESPONSE=$(curl -s -X POST "$API_URL/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"testpass123"}')

TOKEN=$(echo "$RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])" 2>/dev/null)

if [ -z "$TOKEN" ]; then
    echo "✗ Błąd logowania!"
    exit 1
fi

echo "✓ Zalogowano pomyślnie"
echo "  Token: ${TOKEN:0:30}..."

pause

# ─────────────────────────────────────────────────────────────
# 2. TWORZENIE INTEGRACJI
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 2: Tworzenie integracji adresu e-Doręczeń             │"
echo "└─────────────────────────────────────────────────────────────┘"

INTEGRATION=$(curl -s -X POST "$API_URL/api/address-integrations" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "ade_address": "AE:PL-DEMO-'$(date +%s)'-01",
    "provider": "certum",
    "auth_method": "mobywatel",
    "entity_type": "person",
    "pesel": "12345678901"
  }')

INTEGRATION_ID=$(echo "$INTEGRATION" | python3 -c "import sys,json; print(json.load(sys.stdin)['id'])" 2>/dev/null)

echo "$INTEGRATION" | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f'✓ Integracja utworzona')
print(f'  ID: {data[\"id\"]}')
print(f'  Adres: {data[\"ade_address\"]}')
print(f'  Status: {data[\"status\"]}')
"

pause

# ─────────────────────────────────────────────────────────────
# 3. WERYFIKACJA INTEGRACJI
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 3: Weryfikacja integracji                             │"
echo "└─────────────────────────────────────────────────────────────┘"

echo "Kroki weryfikacji:"
curl -s "$API_URL/api/address-integrations/$INTEGRATION_ID/steps" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import sys, json
for step in json.load(sys.stdin):
    icon = {'completed':'✓','in_progress':'⏳','pending':'○'}.get(step['status'],'?')
    print(f'  {icon} {step[\"step\"]}. {step[\"name\"]} [{step[\"status\"]}]')
"

echo ""
echo "Rozpoczynam weryfikację..."
curl -s -X POST "$API_URL/api/address-integrations/$INTEGRATION_ID/verify" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f'  Status: {data[\"status\"]}')
"

pause

# ─────────────────────────────────────────────────────────────
# 4. ZAKOŃCZENIE INTEGRACJI
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 4: Zakończenie integracji                             │"
echo "└─────────────────────────────────────────────────────────────┘"

curl -s -X POST "$API_URL/api/address-integrations/$INTEGRATION_ID/complete" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f'✓ Integracja zakończona!')
print(f'  Status: {data[\"status\"]}')
print(f'  Adres: {data[\"ade_address\"]}')
"

pause

# ─────────────────────────────────────────────────────────────
# 5. WYSYŁANIE WIADOMOŚCI
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 5: Wysyłanie wiadomości                               │"
echo "└─────────────────────────────────────────────────────────────┘"

MESSAGE=$(curl -s -X POST "$API_URL/api/messages" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "recipient": "AE:PL-URZAD-SKAR-BOWY-01",
    "subject": "Testowa wiadomość z demo",
    "content": "To jest treść testowej wiadomości wysłanej podczas demonstracji systemu e-Doręczeń."
  }')

MESSAGE_ID=$(echo "$MESSAGE" | python3 -c "import sys,json; print(json.load(sys.stdin)['id'])" 2>/dev/null)

echo "$MESSAGE" | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f'✓ Wiadomość wysłana!')
print(f'  ID: {data[\"id\"]}')
print(f'  Status: {data[\"status\"]}')
print(f'  Temat: {data[\"subject\"]}')
"

pause

# ─────────────────────────────────────────────────────────────
# 6. LISTA WIADOMOŚCI
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 6: Lista wysłanych wiadomości                         │"
echo "└─────────────────────────────────────────────────────────────┘"

curl -s "$API_URL/api/messages?folder=sent&limit=5" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import sys, json
messages = json.load(sys.stdin)
print(f'Wysłane wiadomości ({len(messages)}):')
for msg in messages:
    print(f'  📤 {msg[\"subject\"]}')
    print(f'     ID: {msg[\"id\"]} | Status: {msg[\"status\"]}')
"

pause

# ─────────────────────────────────────────────────────────────
# 7. STATYSTYKI CQRS
# ─────────────────────────────────────────────────────────────
echo "┌─────────────────────────────────────────────────────────────┐"
echo "│ KROK 7: Statystyki Event Store (CQRS)                      │"
echo "└─────────────────────────────────────────────────────────────┘"

curl -s "$API_URL/api/cqrs/stats" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import sys, json
data = json.load(sys.stdin)
es = data.get('event_store', {})
print(f'Event Store:')
print(f'  Storage: {es.get(\"storage\", \"N/A\")}')
print(f'  Total events: {es.get(\"total_events\", 0)}')
print(f'  Aggregates: {es.get(\"aggregates_count\", 0)}')
print('')
print('Event types:')
for et, count in es.get('event_types', {}).items():
    print(f'  - {et}: {count}')
"

echo ""
echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║     Demonstracja zakończona!                                 ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
echo ""
echo "Możesz teraz używać poszczególnych skryptów:"
echo "  ./01_login.sh           - Logowanie"
echo "  ./02_send_message.sh    - Wysyłanie wiadomości"
echo "  ./03_list_messages.sh   - Lista wiadomości"
echo "  ./08_list_integrations.sh - Lista integracji"
echo ""
